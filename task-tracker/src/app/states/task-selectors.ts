import { createFeatureSelector, createSelector } from "@ngrx/store";
import { Task } from "../models/Task";

// export const selectAllTasks = createSelector(
//   createFeatureSelector("taskEntries"),
//   (state: Task[]) => state
// );
